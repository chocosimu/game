# CLAUDE.md — このプロジェクトの方針書

> このファイルは、今後 Claude が**毎回最初に自動で読み込む**方針書です。
> 作業を始める前に必ずここを読み、内容に沿って進めてください。
> **そして作業の区切りごとに、このファイル（特に「0. いまの状態」）を最新に更新してください。**
>
> 最終更新: 2026-06-02 ／ 状態: **第一段階 完了・公開済み／第二段階に向け「トルネコ3 データ資料」整備中（v3.5）**

## 0. いまの状態（スナップショット）

> ここが「開発状況の正本」です。チャットの記憶があいまいでも、まずここを信じる。

- **段階**: 第一段階「マップ生成＋ターン制移動」**完了**／第二段階「戦闘の土台」に着手（まず**ステータス＋敵配置**まで実装）。
- **遊べる公開URL**: https://chocosimu.github.io/game/ （`main` への反映で自動公開）
- **動いていること**: ダンジョン自動生成／8方向ターン移動（壁の角はすり抜けない）／
  フォグ（霧）／下り階段で次フロア（地形が変わる）／画面ボタン＋キーボード操作。
  **＋HP・満腹度・レベルのHUD表示／満腹度が10歩で1減り0でHP減／スライムをフロアに配置・表示（敵マスには進めない）。**
- **まだ無いもの**: 戦闘（殴り合い・ダメージ・経験値）・敵の移動（今はその場にいるだけ）・アイテム・セーブ機能（再読み込みで最初から）。
- **いま進行中（コード）**: 第二段階「戦闘の土台」。今セッションで **Player(HP/満腹度/Lv/ちから)・Monster(スライム)** を新設し、
  HUD表示・満腹度処理・敵配置・敵マスで停止 を実装（`lib/player.dart`・`lib/monster.dart`・`game_screen.dart`）。テスト22件パス。
- **資料(Wiki)**: データ正本 `docs/torneko3_reference.md`（**v3.5**）。経験値/隠し攻撃力Lv1-99・HP成長(乱数+3〜+7)・
  防御係数35/36・両方向ダメージ式・状態異常・アイテム・全モンスター約186体のLv1基礎値・§0基本ルール
  （指輪1個装備・持ち物20個・ボス=ヘルジャスティス・死亡ペナルティ・倉庫100/持出20・倍速2種・パン回復量・kill-EXP・初期ちから8）。
- **次にやること**: 戦闘の土台の後半 ＝ **敵がターンで動く＋ぶつかると戦闘**（資料のダメージ式で攻撃→HP減→倒すと経験値→レベルアップ）。
  着手前に計画を相談する。残る資料の🔴（基本会心率・矢の攻撃力実数値）は戦闘実装で必要になったら詰める。
- **ブランチ運用**: 作業は `claude/loving-davinci-vXARP` で行い、PR 経由で `main` にマージ → 公開
  （※作業ブランチはセッションごとに変わる。本当の最新は git のリモートブランチを見る）。

## 1. プロジェクトのビジョン（最終目標）

- **「トルネコの大冒険3 不思議のダンジョン」を参考にしたローグライクゲーム**を作る。
- 入るたびに地形が変わる自動生成ダンジョンを、ターン制で潜っていくゲーム。
- **最終的にはスマートフォンで遊べるネイティブアプリ**にする。

## 2. 進め方（とても大事）

- **一気に全部作らない。小さな段階に分けて、少しずつ進める。**
- **大きな変更や新しい段階に入る前には、必ず計画を説明して確認を取る。** いきなり作り始めない。
- 1段階ごとに「動く状態」を保ち、Chrome で動作確認してからコミットする。

## 3. 作業の進め方ルール（状況を見失わないために）★重要

> 長いチャットは何度も「圧縮（要約）」され、決定や経緯が少しずつ抜けて、
> 開発状況がぐちゃぐちゃになりやすい。これを防ぐための約束。

- **1チャット＝1テーマ**。区切りがついたら、**新しいチャット**で続きを始める
  （長く続けるほど要約で情報が失われる）。
- **区切りでは必ず「セーブ」する**。順番は次の3つ：
  1. Chrome で動作確認する
  2. **コミット＆プッシュ**する（git 履歴が正確な記録になる）
  3. **この CLAUDE.md の「0. いまの状態」を更新**する
- **本当の状態は「このファイル＋コード＋git 履歴」**。チャットの要約はあくまで参考。
  もし Claude が状況を取り違えていそうなら、「**git log と実際のコードと CLAUDE.md を見て
  現状を確認して**」と言う（記憶ではなく“実物”を見させる）。
- ここは**使い捨ての環境**。**保存（コミット）していない変更は消える**ので、こまめにコミットする。
- この CLAUDE.md は毎セッション自動で読まれる。だから**常に最新に保つ**こと自体が最大の対策。

## 4. 作者について / 説明のしかた

- 作者は**プログラミングがほぼ初心者**。
- **専門用語はそのまま使わず、かみ砕いて説明する**（例：「タイル＝方眼紙の1マス」）。
- なぜそう作るのか、何をしているのかを、日本語でやさしく説明する。
- コード内のコメントも、要点は日本語で分かりやすく書く。

## 5. 技術スタック

- **Flutter / Dart**（1つのコードでスマホにもWeb（Chrome）にも出せる）。
- 第一段階の描画は **Flutter標準の CustomPaint**（色付きのマスで表現）。
  本格的な絵・アニメが必要になったら、ゲームエンジン **Flame** の導入を検討する。
- 動作確認は当面 **Chrome（Web）** で行う。最後の段階でスマホ向けに仕上げる。

## 6. 現在の段階の中身（第一段階）

**マップ生成 ＋ 移動の土台** ✅ 実装済み・公開済み

- 入るたびに地形が変わる自動生成ダンジョン（区画→部屋→通路）
- キャラクターがターン制で1マスずつ移動（8方向／壁の角はすり抜けない）
- フォグ（霧）：歩いて見た所だけ明るく、通った所は記憶して薄く表示（トルネコ風の視界）
- 下り階段に乗ると次のフロアへ（地形が作り直される）
- 画面のボタン操作（＋PC確認用にキーボード矢印 / WASD / QEZC）

> 敵・アイテム・戦闘は**まだ入れない**。この段階では土台作りに集中する。

## 7. これからのロードマップ（予定。各段階の前に相談する）

1. ✅ **第1段階**：マップ生成＋移動（完了・公開済み）
2. 第2段階：視界の調整・ミニマップ・階段まわりの演出
3. 第3段階：アイテム（落ちている／拾う／足元に置く）
4. 第4段階：敵キャラの登場＋敵もターンで動く
5. 第5段階：戦闘（攻撃／HP）
6. 第6段階：満腹度・レベル・装備など、トルネコらしい要素
7. 第7段階：スマホ向けネイティブアプリとして仕上げる

> ほかに「進行のセーブ」など、途中段階で入れたい機能が出たら相談して決める。

## 8. ディレクトリ構成

```
lib/
  main.dart          アプリの入口
  dungeon.dart       ダンジョンの地図データと自動生成ロジック
  player.dart        プレイヤーの状態（HP・満腹度・レベル・ちから）
  monster.dart       モンスターの種類（設計図）と1匹分のデータ
  game_screen.dart   画面の描画・入力・ターン管理（フォグ・HUD・敵配置含む）
  legacy/
    tic_tac_toe.dart  最初に試作した○×ゲーム（本筋ではないが保管）
test/
  dungeon_test.dart       生成された地図が正しいかのテスト
  player_test.dart        HP・満腹度・レベルのテスト
  monster_test.dart       スライムのデータのテスト
  game_screen_test.dart   移動・HUD表示の基本動作テスト
  legacy/                 ○×ゲームのテスト
.github/workflows/
  deploy-pages.yml   main への反映で自動公開（GitHub Pages）
CLAUDE.md            この方針書
docs/
  torneko3_reference.md  トルネコ3 システムのデータ正本（Wiki）
```

> ※ `lib/legacy/tic_tac_toe.dart` は試作品。消さずに保管。単独起動は
> `flutter run -d chrome -t lib/legacy/tic_tac_toe.dart`。

## 9. 公開（デプロイ）の仕組み

- `.github/workflows/deploy-pages.yml` が、**`main` への push をきっかけに自動で**
  「Flutter で Web ビルド → GitHub Pages へ公開」まで行う。
- 公開URL: https://chocosimu.github.io/game/ （プロジェクトページなので `base-href "/game/"`）。
- CanvasKit（描画エンジン）は `--no-web-resources-cdn` で**ローカル同梱**。
- 公開は **`main` ブランチからのみ**（GitHub の初期設定）。だから作業ブランチを
  `main` にマージすると、新しい内容が自動で公開される。

## 10. よく使うコマンド

```bash
flutter pub get                 # 依存パッケージの取得
flutter run -d chrome           # Chrome で起動（開発・確認）
flutter analyze                 # コードの静的チェック
flutter test                    # 自動テスト
flutter build web --release --no-web-resources-cdn   # Web用ビルド
```

> このネットワーク環境では、CanvasKit を CDN から取得できないことがある。
> Webビルド・確認時は **`--no-web-resources-cdn`** を付けてローカル同梱にすること。
